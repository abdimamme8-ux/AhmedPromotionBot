services:
  - type: web
    name: AhmedPromotionBot
    env: python
    runtime: python-3.12
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
        sync: false
